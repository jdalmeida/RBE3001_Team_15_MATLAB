function [a0, a1, a2, a3] = TrajectoryGen(t0, tf, v0, vf, q0, qf)
    
    % Matrix of the final and initial t values
    t = [1, t0, t0^2, t0^3;...
         0, 1, 2*t0, 3*t0^2;...
         1, tf, tf^2, tf^3;...
         0, 1, 2*tf, 3*tf^2];
     
     % Initial and Final positions and velocities
     posVel = [q0; v0; qf; vf];
     
     
     a = 

end

